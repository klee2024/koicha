services:
  db:
    image: postgres:16
    container_name: koicha-db
    environment:
      POSTGRES_DB: koicha
      POSTGRES_USER: koicha
      POSTGRES_PASSWORD: koicha
    ports:
      - "5432:5432"
    volumes:
      - koicha_pgdata:/var/lib/postgresql/data

volumes:
  koicha_pgdata:
